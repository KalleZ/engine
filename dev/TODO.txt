

	Tuxxedo Engine 2.0.0
	----------------------

	Improve development tools
	 - Add internationalization support
	 - Add styles support
	 - Add user support
	 - Re-implement missing tools
	   - Options
	   - Passwords
	   - Templates

	Implement the $storage/globals/instances parameter to the registry

	Add a CHANGELOG file for all the improvements from 1.0 to 2.0
	Add a new option to the datamanagers to allow developers to override optional fields and marking them as required
	Add a new interface for datamanagers to let the code know that this class rebuilds the datastore
	Add the ability to format messages in formdata exceptions

	Rewrite the email validator, so we have our very own

	Remove the functions_*.php files and their associated templates


	Tuxxedo Engine Future
	----------------------

	Add new datamanagers
	 - Language
	 - Permission
	 - Phrase
	 - Phrasegroup
	 - Template

	Add a generic pagination class with a default Engine database pagination driver
	Add cronjobs and move the related user/session code into its own cronjob

	Implement a temporary memory storage, with backends for APC etc.

	Change SQL queries to be non-mysql compatible
	Change the class names, and remove the 'Tuxxedo_' prefix
	Change to use namespaces, or atleast have a plan and make the code ready for it without breaking compatibility