{$header}

" . ((isset($categories)) ? ("
{$categories}
") : '') . "

" . (($orphan) ? ("
<div class=\"box\">
	<h4>Orphan options</h4>

	<p>
		Orphan options are options thats not assigned to a valid 
		category, or no category at all. Note that this is a virtual 
		category and it will disapear once all below options have 
		a valid category.
	</p>
	<br />

	<table cellspacing=\"0\" cellpadding=\"0\">
		<tr class=\"heading\">
			<td class=\"start\" style=\"width: 137px;\">
				Name
			</td>
			<td style=\"width: 193px;\">
				Value
			</td>
			<td nowrap=\"nowrap\" style=\"width: 150px;\">
				Default value
			</td>
			<td style=\"width: 62px;\">
				Cached?
			</td>
			<td nowrap=\"nowrap\" style=\"width: 108px;\">
				Datastore value
			</td>
			<td class=\"end\">
				&nbsp;
			</td>
		</tr>
		{$rows}
	</table>
</div>
") : '') . "

" . (($reminder) ? ("
<div class=\"warning\">
	<h3>Warning</h3>

	<p>
		Not all elements are cached in the datastore or some may be 
		outdated, you need to rebuild the datastore for these changes 
		to take effect.
	</p>
</div>
") : '') . "

{$footer}